# .env.example

# --- 选择一种数据库配置 ---

# 方案 A: 本地 Docker 数据库配置
# 如果使用 Docker, 请取消下面 4 行的注释
# DATABASE_HOST=localhost
# DATABASE_PORT=5432
# DATABASE_USER=your_db_user
# DATABASE_PASSWORD=your_db_password
# DATABASE_NAME=dat_view_db

# 方案 B: Supabase 云数据库配置
# 如果使用 Supabase, 请取消下面 1 行的注释，并替换为你的连接字符串
# DATABASE_URL="postgresql://postgres:[YOUR-PASSWORD]@[AWS-REGION][.pooler.supabase.com:6543/postgres](https://.pooler.supabase.com:6543/postgres)"

# --- JWT 安全密钥 (必须填写) ---
# 请务必使用强随机字符串替换!
JWT_ACCESS_SECRET=your_super_secret_for_access_token
JWT_REFRESH_SECRET=your_super_secret_for_refresh_token